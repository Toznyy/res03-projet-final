<h1>Inscription</h1>

<!-- Affiche un message d'erreur si nécessaire -->
<section>
    <?php if (isset($data["errorMessage"]) && !empty($data["errorMessage"])) { ?>
        <p><?php echo $data["errorMessage"] ?></p>
    <?php } ?>
</section>

<!-- Formulaire pour créer un nouveau compte utilisateur -->
<form method="post" action="creation">

    <!-- Champ pour le pseudo de l'utilisateur client -->
    <fieldset>
        <label for="username">Pseudo</label>
        <input type="text" name="username" id="username" />
    </fieldset>

    <!-- Champ pour l'adresse e-mail de l'utilisateur -->
    <fieldset>
        <label for="email">Email</label>
        <input type="text" name="email" id="email" />
    </fieldset>

    <!-- Champs pour le prénom et le mot de passe de l'utilisateur -->
    <section>
        <fieldset>
            <label for="firstname">Prénom</label>
            <input type="text" name="first_name" id="firstname" />
        </fieldset>
        <fieldset>
            <label for="password">Mot de passe</label>
            <input type="password" name="password" id="password" />
        </fieldset>
    </section>

    <!-- Champs pour le nom et la confirmation du mot de passe de l'utilisateur -->
    <section>
        <fieldset>
            <label for="lastname">Nom</label>
            <input type="lastname" name="last_name" id="lastname" />
        </fieldset>
        <fieldset>
            <label for="confirmpassword">Confirmation mot de passe</label>
            <input type="password" name="confirmpassword" id="confirmpassword" />
        </fieldset>
    </section>

    <!-- Champ pour la date d'anniversaire de l'utilisateur -->
    <fieldset>
        <label for="birthday">Anniversaire</label>
        <input type="date" name="birthday" id="birthday" />
    </fieldset>

    <!-- Champ caché pour indiquer que le formulaire de création a été soumis -->
    <fieldset>
        <input type="hidden" name="formName" value="creation">
    </fieldset>

    <!-- Bouton pour soumettre le formulaire -->
    <fieldset>
        <input type="submit" value="Je crée mon compte" />
    </fieldset>

    <!-- Lien pour se connecter à un compte existant -->
    <p>Clique <a href="../connexion/connexion.phtml">ici</a> pour te connecter</p>
</form>
